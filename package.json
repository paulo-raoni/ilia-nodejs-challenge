{
  "name": "ilia-nodejs-challenge",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "eslint .",
    "format": "prettier -w .",
    "format:check": "prettier -c .",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down -v",
    "token:external": "node scripts/generate-external-token.js user-123",
    "token:internal": "node scripts/generate-internal-token.js user-123"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^8.57.0",
    "globals": "^15.0.0",
    "jest": "^29.7.0",
    "prettier": "^3.0.0"
  }
}
